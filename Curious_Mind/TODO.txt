- fix back navigation issue with ProfileScreen and ResetPasswordScreen
- test ALL firebase calls (DbLogic file)
- refactor resetPassword function in DbLogic to helper functions (only keep the DB calls)
- sendForgotPasswordEmail in DbLogic needs to be done better and tested
- handleSignUp in DbLogic needs to be refactored into helper functions
- input validations all over the app (and check that an event/post can't be created without certain inputs)
- search for users
- filter posts
- Add regex to filters 
- fix distance between "pastor" tag and report number
- logo is left justified on android
- new post options screen are all left justified
- reporting a post from the thread screen and having it get deleted, then making a new post results in:
    "ERROR  Warning: Can't perform a React state update on an unmounted component. This is a no-op, but 
    it indicates a memory leak in your application. To fix, cancel all subscriptions and asynchronous tasks in the componentWillUnmount method."
